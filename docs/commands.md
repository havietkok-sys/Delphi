### Git commandon jag ofta använder 
git status
git add .
git commit -m "..."
git push
git pull
git switch main
git switch dev


### Större lathund

# Git

## Kolla läget

git status

## Spara arbete

git add .
git commit -m "beskrivning"

## Skicka till GitHub

git push

## Hämta senaste

git pull

## Se branches

git branch

## Byt branch

git switch main
git switch dev

## Skapa ny branch

git switch -c ny-branch

## Se historik

git log --oneline

---

# Första pushen av en ny branch

git push -u origin branch-namn


# "Logga" in på Env

.venv\Scripts\Activate.ps1